services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    env_file:
      - .env
    command: python manage.py runserver 0.0.0.0:$PORT
    ports:
      - "8000:8000"
    volumes:
      - .:/app